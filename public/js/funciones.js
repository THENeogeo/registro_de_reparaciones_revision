// Estas funciones son simplemente wrappers de las funciones de storage
// para mantener una capa de abstracción

function obtenerPersonas() {
    return obtenerPersonas();
}

function obtenerPersonaPorId(id) {
    return obtenerPersonaPorId(id);
}

function agregarPersona(persona) {
    agregarPersona(persona);
}

function actualizarPersona(persona) {
    actualizarPersona(persona);
}

function borrarPersona(id) {
    borrarPersona(id);
}